datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Watchlist {
  id     String      @id @default()
  movies Movie[]
}

model Movie {
  tmdbId  Int      @unique
  title   String
  year    Int
  poster  String
}
